@model string
@{
    ViewData["Title"] = "Aviso";
}

<div class="alert alert-warning mt-4 text-center">
    <h4>@Model</h4>
    <a asp-action="Index" asp-controller="Home" class="btn btn-primary mt-3">Volver al inicio</a>
</div>
